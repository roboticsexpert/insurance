node_modules
**/node_modules
**/dist
**/build
**/.astro
**/.wrangler
**/.turbo
.git
.claude
docs
tools
**/.env
**/.env.*
!**/.env.example
**/*.log
.DS_Store

# apps/web source is needed by apps/web/Dockerfile. apps/docs deploys to Cloudflare
# and no image builds it — but pnpm reads every package.json named in
# pnpm-workspace.yaml before it applies --filter, so its manifest has to stay.
apps/docs
!apps/docs/package.json
