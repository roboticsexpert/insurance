{
  "name": "@bimegold/brand",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "../../tools/brand-gold/.venv/bin/python ../../tools/brand-gold/site.py",
    "deploy": "pnpm build && wrangler deploy",
    "dev": "pnpm build && wrangler dev --port 8788"
  },
  "devDependencies": {
    "@fontsource-variable/vazirmatn": "^5.3.0",
    "wrangler": "^4.124.0"
  }
}
